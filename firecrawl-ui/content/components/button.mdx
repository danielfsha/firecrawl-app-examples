---
title: Button
description: Displays a button or a component that looks like a button.
---

import { Button } from "@/components/ui/button"
import { Preview } from "@/components/mdx/preview"
import { CodeInstall } from "@/components/ui/code-install"
import { ArrowRight } from "lucide-react"

# Button

Displays a button or a component that looks like a button.

<Preview code={`import { Button } from "@/components/ui/button";

<Button variant="default">Primary</Button>
<Button variant="secondary">Secondary</Button>
<Button variant="outline">Outline</Button>
<Button variant="ghost">Ghost</Button>
<Button variant="destructive">Destructive</Button>`}>
  <div style={{ display: "flex", gap: "8px", flexWrap: "wrap" }}>
    <Button variant="default">Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="ghost">Ghost</Button>
    <Button variant="destructive">Destructive</Button>
  </div>
</Preview>

## Installation

<CodeInstall
  tabs={[
    { label: "npm", code: "npm install firecrawl" },
    { label: "pnpm", code: "pnpm add firecrawl" },
    { label: "yarn", code: "yarn add firecrawl" },
    { label: "bun", code: "bun add firecrawl" },
  ]}
/>

## Usage

<Preview code={`import { Button } from "@/components/ui/button";

<Button variant="outline">Button</Button>`}>
  <Button variant="outline">Button</Button>
</Preview>

## Examples

### Size

<Preview code={`<Button size="xs">Extra Small</Button>
<Button size="sm">Small</Button>
<Button size="default">Default</Button>
<Button size="lg">Large</Button>`}>
  <div style={{ display: "flex", gap: "8px", alignItems: "center", flexWrap: "wrap" }}>
    <Button size="xs">Extra Small</Button>
    <Button size="sm">Small</Button>
    <Button size="default">Default</Button>
    <Button size="lg">Large</Button>
  </div>
</Preview>

### Outline

<Preview code={`<Button variant="outline">Outline</Button>`}>
  <Button variant="outline">Outline</Button>
</Preview>

### Secondary

<Preview code={`<Button variant="secondary">Secondary</Button>`}>
  <Button variant="secondary">Secondary</Button>
</Preview>

### Ghost

<Preview code={`<Button variant="ghost">Ghost</Button>`}>
  <Button variant="ghost">Ghost</Button>
</Preview>

### Destructive

<Preview code={`<Button variant="destructive">Delete</Button>`}>
  <Button variant="destructive">Delete</Button>
</Preview>

### Link

<Preview code={`<Button variant="link">Link Button</Button>`}>
  <Button variant="link">Link Button</Button>
</Preview>

### Icon

<Preview code={`import { ArrowRight } from "lucide-react";

<Button size="icon">
  <ArrowRight />
</Button>`}>
  <Button size="icon">
    <ArrowRight />
  </Button>
</Preview>

### With Icon

<Preview code={`<Button size="lg">
  Get Started
  <ArrowRight data-icon="inline-end" />
</Button>`}>
  <Button size="lg">
    Get Started
    <ArrowRight data-icon="inline-end" />
  </Button>
</Preview>

## API Reference

### Button

| Prop | Type | Default |
|------|------|---------|
| `variant` | `"default" \| "outline" \| "ghost" \| "destructive" \| "secondary" \| "link"` | `"default"` |
| `size` | `"default" \| "xs" \| "sm" \| "lg" \| "icon" \| "icon-xs" \| "icon-sm" \| "icon-lg"` | `"default"` |

{/* ### buttonVariants

A `cva` helper function for styling non-button elements as buttons.

```tsx
import { buttonVariants } from "@/components/ui/button";

<a className={buttonVariants({ variant: "outline", size: "sm" })}>
  Link
</a>
``` */}
