---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
---

import { Switch } from "@/components/ui/switch";
import { Preview } from "@/components/mdx/preview";
import { CodeInstall } from "@/components/ui/code-install";
import { CodeBlock } from "@/components/ui/code-block";
import { ExpandableCode } from "@/components/expandable-code";
import { Steps, Step } from "@/components/ui/steps";
import {
  Tabs,
  TabsList,
  TabsTrigger,
  TabsDivider,
  TabsContent,
} from "@/components/ui/tabs";

# Switch

A control that allows the user to toggle between checked and not checked.

<Preview code={`import { Switch } from "@/components/ui/switch";

<Switch />`}>
  <div style={{ display: "flex", gap: "12px", alignItems: "center" }}>
    {/* <Switch /> */}
    <Switch defaultChecked />
    Airplane mode
  </div>
</Preview>

## Installation

<Tabs defaultValue="command"  className='py-4'>
  <TabsList  className='my-3'>
    <TabsTrigger value="command">Command</TabsTrigger>
    <TabsDivider />
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="command">
    <CodeInstall
      tabs={[
        { label: "npm", code: "npx shadcn@latest add switch" },
        { label: "pnpm", code: "pnpm dlx shadcn@latest add switch" },
        { label: "yarn", code: "npx shadcn@latest add switch" },
        { label: "bun", code: "bun dlx shadcn@latest add switch" },
      ]}
    />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step title="Install dependencies">
        <CodeInstall
          tabs={[
            { label: "npm", code: "npm install @base-ui/react" },
            { label: "pnpm", code: "pnpm add @base-ui/react" },
            { label: "yarn", code: "yarn add @base-ui/react" },
            { label: "bun", code: "bun add @base-ui/react" },
          ]}
        />
      </Step>
      <Step title="Copy the component">
        <ExpandableCode>
          <CodeBlock code={`"use client";

import { Switch as SwitchPrimitive } from "@base-ui/react/switch";
import { cn } from "@/lib/utils";

function Switch({
className,
size = "default",
...props
}: SwitchPrimitive.Root.Props & {
size?: "sm" | "default";
}) {
return (

<SwitchPrimitive.Root
data-slot="switch"
data-size={size}
className={cn(
"peer group/switch relative inline-flex shrink-0 items-center rounded-full border border-transparent transition-all data-[size=default]:h-[20px] data-[size=default]:w-[50px] data-[size=sm]:h-[14px] data-[size=sm]:w-[24px] data-checked:bg-primary data-unchecked:bg-input data-disabled:cursor-not-allowed data-disabled:opacity-50",
className
)}
{...props} >
<SwitchPrimitive.Thumb
data-slot="switch-thumb"
className="pointer-events-none block rounded-full bg-background ring-0 transition-transform group-data-[size=default]/switch:h-[16px] group-data-[size=default]/switch:w-[28px] group-data-[size=sm]/switch:size-3"
/>
</SwitchPrimitive.Root>
);
}

export { Switch };`} language="tsx" showLineNumbers={true} />
        </ExpandableCode>
      </Step>
      <Step title="Update imports">
        Update the import paths to match your project setup.
      </Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

<Preview code={`import { Switch } from "@/components/ui/switch";

<Switch />`}>
  <Switch />
</Preview>

## Examples

### Default

<Preview code={`<Switch />`}>
  <Switch />
</Preview>

### Checked

<Preview code={`<Switch defaultChecked />`}>
  <Switch defaultChecked />
</Preview>

### Disabled

Add the `disabled` prop to the Switch component to disable the switch.

<Preview
  code={`<Switch disabled />
<Switch disabled defaultChecked />`}
>
  <div style={{ display: "flex", gap: "24px", alignItems: "center" }}>
    <Switch disabled />
    <Switch disabled defaultChecked />
  </div>
</Preview>

### Size

Use the `size` prop to change the size of the switch.

<Preview
  code={`<Switch size="sm" />
<Switch size="default" />
<Switch size="sm" defaultChecked />
<Switch size="default" defaultChecked />`}
>
  <div style={{ display: "flex", gap: "24px", alignItems: "center" }}>
    <Switch size="sm" />
    <Switch size="default" />
    <Switch size="sm" defaultChecked />
    <Switch size="default" defaultChecked />
  </div>
</Preview>

## API Reference

### Switch

| Prop              | Type                         | Default     |
| ----------------- | ---------------------------- | ----------- |
| `defaultChecked`  | `boolean`                    | `false`     |
| `checked`         | `boolean`                    | —           |
| `onCheckedChange` | `(checked: boolean) => void` | —           |
| `disabled`        | `boolean`                    | `false`     |
| `size`            | `"sm" \| "default"`          | `"default"` |
| `className`       | `string`                     | —           |

See the [Base UI Switch documentation](https://base-ui.com/react/components/switch) for more information.
